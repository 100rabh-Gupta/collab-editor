# Client

The client currently contains a static M0 landing page served by Nginx. The React, TypeScript, CRDT, networking, and editor layers will be implemented in milestones M1–M5.

The placeholder can be previewed through the local Docker Compose stack with `make dev`.
