# Deployment

Production deployment configuration will be added in milestone M6. Local development uses the root `docker-compose.yml`; production will add a dedicated Compose file and Fly.io configuration.
