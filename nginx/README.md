# Nginx configuration

`nginx.conf` is the M0 reverse-proxy scaffold. It serves the client and reserves `/ws/` for the future WebSocket backend. TLS, timeouts, security headers, and production hardening are scheduled for M6.
